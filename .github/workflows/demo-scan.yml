name: Q-shield Demo Scan

on:
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          cd 3_scanner
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      # [✅ 추가됨] 테스트 폴더를 Git 저장소로 강제 변환하는 단계
      - name: Initialize local test repo
        run: |
          # 테스트 폴더가 존재하는지 확인
          if [ -d "test_vulnerable_repo" ]; then
            echo "📂 test_vulnerable_repo 폴더 발견. Git 저장소로 초기화합니다..."
            cd test_vulnerable_repo
            
            # Git 초기화 및 커밋 (git clone이 가능하도록 만듦)
            git init
            git config user.email "action@github.com"
            git config user.name "GitHub Action"
            git add .
            git commit -m "Initialize local repo for testing"
            
            echo "✅ Git 초기화 완료."
          else
            echo "❌ test_vulnerable_repo 폴더가 없습니다. 경로를 확인해주세요."
            ls -R
            exit 1
          fi

      - name: Run scanner against local test repo
        working-directory: ./3_scanner
        run: python demo_run.py
